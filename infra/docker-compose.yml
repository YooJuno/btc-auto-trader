services:
  db:
    image: postgres:16
    container_name: btc-trader-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-btctrader}
      POSTGRES_USER: ${POSTGRES_USER:-btctrader}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-btctrader}
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
